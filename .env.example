DEEPSEEK_API_KEY=sk-
PLAYWRIGHT_HEADLESS=false
MAX_RESULTS=5
SEARCH_TIMEOUT=60

# 抖音 Cookie（从 Chrome DevTools → Network → 任意抖音请求 → Request Headers → cookie 复制整行）
# 或 Application → Cookies → douyin.com，全选复制
# 用双引号包裹整个值
DOUYIN_COOKIES=""